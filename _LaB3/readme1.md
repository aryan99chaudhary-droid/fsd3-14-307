any API can be of 2 type
1. pre -> create
2. post -> update

api can be response by server with status code AND jason
by default browser can check only get request , to check other three request time like post fetch and  we require frontend or third party api tester like postmal, hunger client , eco api, etc .
